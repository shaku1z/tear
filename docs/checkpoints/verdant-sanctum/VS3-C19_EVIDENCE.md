# VS3-C19 TearBench completion and anti-drift evidence

## Outcome

`VS3-C19` is **GREEN** at gated Tear source
`98c4134f6fd0bcbc670315ff62dc80b805efb1ca`.

Verdant production identities, scenarios, environment state, causal facts,
progression, audio disposition, and reference projections are covered by
source-derived authority and diff-aware evidence selection. The selected
commands execute real unit, build, browser, State Forge, boss, and parity
evidence. This is an engineering/non-certifying completion statement; C40 is
unchanged and Verdant publication remains prohibited.

## Source-derived authority and selection

- The clean selection is bound to source fingerprint
  `754857db78ff57857d99d345eb4ba6b7f4b4de1650fffa245dad5c00d6c3bd96`
  with an empty worktree fingerprint.
- Ten representative changed authorities select twelve routes: audio,
  current environment/runtime authority, current-game authority, movement,
  progression, shared runtime, Rootbound Phase II, environment codec/kernel,
  Bloom Well, Rootbinder network, and Verdant variant selection.
- The resulting catalog executes 24 subject-correct scenarios, including the
  six source-derived boss/stage pairs, all five current weapons, natural
  Rootbound in Verdant Sanctum, Bloom Well, root-network sever, Graft
  destruction, and explicit variant selection.
- Selection anti-drift tests fail closed for missing/duplicate/wrong-stage
  bosses, missing active weapons, retired identities, wrong subject or start,
  unsupported backends, unsafe commands, stale build identity, and dirty
  source receipts.
- Rootbinder remains the twelfth production enemy identity and has no variant.
  Static Bloom remains rejected; the engineering-only fallback disposition is
  selected by the audio route, while replacement music remains deferred to
  `VS3-C22-S5`.

## Replay and restore correction

C19 found two retained C30 terminal fixtures whose Rootbinder-era semantic and
content hashes were current but whose configuration hashes were stale. Only
those two provenance fields were corrected; their complete natural traces now
match the production headless environment exactly.

C19 also reproduced a real run/rerun defect. Restoring a tick-zero State Forge
snapshot invalidated wave-actor ownership even when the restored world proved
that its current wave contained no living actors. The live environment now
resumes ownership only for that empty-wave case and rebuilds the set from
subsequent native spawn/death facts. Non-empty restored snapshots remain
unavailable rather than guessing actor ownership.

The retained `verdant-bloom-well-cycle` run and immediate rerun both pass at
720 fixed ticks with seed `vs3-c19-bloom-well-cycle`, build artifact
`7010878543fff27026b1a1cf234efaa8deba5a9f56905cf4f5de5dfed7bd7494`,
and terminal semantic hash `1f508a7db3d79719`.

## Capability and observation boundaries

- Live Class-A browser evidence covers current gameplay, natural Rootbound,
  Bloom Well, Rootbinder links, Grafts, variants, and State Forge paths.
- Portable live/detached behavior is claimed only where the registered
  backend and parity suite execute it. The focused comparison passes for the
  current shared fixed-step behavior.
- Rootbound's natural encounter, Rootbinder network surgery, Graft surgery,
  and variant selection remain registered as live-only where declared; no
  detached support is invented.
- Invariants have meaningful positive and negative proofs. Wave completion
  fails closed when source-owned current-wave actor evidence is unavailable.
- Browser artifacts use privileged/structured engineering observation. No
  Class-C pixel/physical-input certification, release readiness, public
  promotion, or C40 result is claimed.

## Raw evidence bundle

Regenerable raw evidence is retained under the ignored canonical path
`artifacts/tearbench/checkpoints/verdant-sanctum/VS3-C19/`:

- `changed-files.txt`
- `evidence-selection.json`
- `current-capability.json`
- `verdant-bloom-well-cycle.run.json`
- its action trace and screenshot

The checkpoint-owned capability artifact prevents later scratch executions
under `artifacts/tearbench/generated/` from obscuring C19 lineage.

## Validation

| Command | Result |
| --- | --- |
| `pnpm requirements:check` | PASS; 8,691 requirements, zero unmapped source lines. |
| `pnpm typecheck` | PASS. |
| `pnpm lint` | PASS. |
| `pnpm check:architecture` | PASS. |
| `pnpm test:tearbench-selection` | PASS, 24 anti-drift tests. |
| `pnpm check:active-roster` | PASS; exact Final Five, 55 files scanned. |
| C19 minimum focused authority suite | PASS, 4 files / 41 tests. |
| Selected C19 authority response | PASS, 32 files / 203 tests. |
| `pnpm build:test:standalone` | PASS; exact source and artifact attribution above. |
| `pnpm test:browser:current-gameplay-scenarios` | PASS, 13 source-owned scenarios. |
| `pnpm test:browser:bosses` | PASS, including Rootbound in Verdant Sanctum. |
| `pnpm test:browser:state-forge` | PASS; 600-tick restore, Studio host, wave 99, 15 boss phases, 39 clean-runtime launches. |
| `pnpm test:browser:current-live-detached-parity` | PASS. |
| `pnpm tearbench run verdant-bloom-well-cycle ...` plus `rerun` | PASS/PASS at the exact source/build above. |
| `pnpm tearbench ci ... --artifact .../VS3-C19/current-capability.json` | PASS; selected commands and graveyard gate executed. |

## Boundaries

No source, evidence, build, or reference artifact was published, dispatched,
promoted, deployed, or certified. Replacement music and its rights/vendor
package remain deferred to `VS3-C22-S5`. Verdant publication remains
prohibited and C40 status is unchanged.
