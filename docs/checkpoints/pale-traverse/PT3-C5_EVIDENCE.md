# PT3-C5 — stage and presentation

## Claim

The Pale Traverse is an engineering-only campaign stage at waves 41–50. Its
natural wave-41 entry, chapter transition, horizontal layout, stage-owned
Aurora Tracks, frozen-mountain presentation, snow/ice platform material,
accessibility policies, and temporary music fallback use the existing campaign,
environment, presentation, State Forge, and audio authorities. It remains
explicitly non-public and does not project an incomplete White Hart into the
boss or game-reference catalogs.

## Canonical result

- `pale-traverse` is stage V between Verdant Sanctum and Voidspire. The
  campaign roster now has seven stages, while the comparison curve remains
  provisional until PT3-C8 and the separately authorized joint balance gate.
- Natural progression from wave 40 enters stage index 4, presents chapter V,
  loads the Pale layout, activates three directional stage-owned Aurora Tracks,
  and admits only the local-wave-one Pale pool.
- The presentation route draws a coral-dusk frozen passage with a central
  mountain landmark, restrained aurora, edge pines, village lights, bounded
  lower-field reflections, sparse snow, deep navy platforms, snow caps, cyan
  ice edges, sparse icicles, and fractures.
- Aurora Tracks render their immutable direction facts as warned route lanes.
  High-contrast/reduced-motion/low-graphics mode preserves static direction
  geometry without depending on animation, particles, audio, or flash.
- State-owned environment references may resolve a canonical stage only on a
  hazard `ownerId` edge. Target/source links and invented stage-like IDs still
  fail the shared identity graph.
- `fillet` is the engineering fallback for Pale. No public Pale or White Hart
  music rule was added; final music selection remains deferred.
- `config/campaign-publication-boundary.json` names the exact seven active
  engineering stages and still refuses publication because its status remains
  `engineering-only` with Verdant and Pale jointly required.

## Frozen source and browser evidence

- Completion identity: `d49b1e7f65c7afbbb6fae4c30025d8bc2f510202`
- Standalone artifact hash:
  `94473b61bd16f3c443d7561a2c8bc1667eafb83fe94b7195e0b11ef789d1fd1f`
- Build source state: clean
- Regenerable evidence manifest:
  `artifacts/tearbench/checkpoints/pale-traverse/PT3-C5/presentation/evidence.json`
- Captures: desktop 1600×900, laptop 2048×1024, 1200×900 shape,
  touch-landscape 896×414, plus desktop and touch high-contrast,
  reduced-motion, low-graphics, audio-off variants.

The browser journey enters the source-authored Wave 41 scenario through the
production State Forge transaction, asserts stage index 4 and three alternating
Track directions, and renders the real live world. Because the detached State
Forge renderer is not the active browser input owner, the unrelated DOM
pointer-lock hint is suppressed by test-only CSS; the canvas frame is otherwise
unaltered. This is engineering evidence, not public certification.

## Verification

```text
pnpm typecheck
pnpm exec eslint <PT3-C5 changed TypeScript files>
pnpm exec vitest run <18 focused PT3-C5/adjacent files>
pnpm check:architecture
pnpm check:active-roster
pnpm check:docs
pnpm check:terminology
pnpm requirements:check
pnpm build:test:standalone
pnpm test:browser:pale-presentation
git diff --check
```

Result: 18 focused files / 86 tests passed after the shared stage-owner
reference correction. Typecheck, targeted lint, architecture, active roster,
documentation, terminology, requirements, whitespace, clean standalone build,
and six-profile browser evidence passed.

The clean-source game-reference exporter intentionally refuses this checkpoint:
its six-way boss/stage bijection cannot admit Pale until White Hart exists.
PT3-C11 owns the exact seven-way reference projection. This expected
fail-closed boundary prevents publication of an incomplete stage rather than
constituting a C5 regression.

## Evidence boundary

The assembled biome is locally visible and campaign-reachable at Wave 41, but
White Hart is still identity-only. PT3-C6 and PT3-C7 own its factory/encounter
foundation and three-phase behavior. Full campaign composition, modes,
persistence, exact reference/TearBench coverage, and final freeze validation
remain PT3-C8 through PT3-C11. Joint integration, public reference dispatch,
final music selection, deployment, and C40 certification remain prohibited.
