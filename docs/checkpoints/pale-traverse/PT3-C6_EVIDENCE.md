# PT3-C6 — White Hart foundation

## Claim

The White Hart now has one coherent engineering-only production composition
through the existing boss definition, world-entity factory, placement,
encounter, living-arena, Boss Test, structured observation, codec/hydration,
result, retry, and cleanup paths. No third boss registry or placeholder attack
damage was introduced. Every named White Hart attack remains explicitly
unavailable until PT3-C7.

## Canonical result

- `WHITE_HART_PROVISIONAL_DEFINITION` is the single executable boss definition
  projected into `BOSS_ROSTER`, the seven-stage boss/reference bijection,
  Boss Test choices, TearBench boss registries, and phase launch matrices.
- The stable `white-hart` world factory constructs the same `WhiteHart` family
  in live and detached production worlds. It never falls back to the generic
  `boss` constructor.
- Source-owned `CONFIG.whiteHart` defines a 190×104 low quadruped body with
  2,500 HP, real collision/contact damage, pursuit-ready base speed, and boss
  weight/knockback policy. The exact grounded transform comes from the shared
  boss-placement authority.
- The shared encounter path installs the Pale living arena with `pale-ice`
  material, freezes the intro, and preserves the existing arena lifecycle.
- The foundation owns monotonic phase ordinals 1–3, the authored phase labels,
  intro, idle/recovery, damage gating, non-humanoid silhouette, and phase-aware
  shell fracture presentation. Its available attack list is empty and the
  shared attack-availability projection returns `unavailable` for all phases.
- Current structured observation derives `white-hart`, phases 1–3, and
  `pale-traverse` from production authorities. The dedicated canonical live
  scenario and browser journey use the real Boss Test constructor.
- Boss cleanup is idempotent on death, reset, retry, exit, stage transition,
  and restore. Stage transitions now invoke the shared boss-cleanup boundary
  before loading the replacement stage. Failed restore remains fail-closed
  before world replacement; successful restore uses the same `restore` cleanup.
- The local game-reference exporter now proves the exact seven-way
  stage/boss projection. This is an engineering check only; no artifact was
  dispatched or published.

## Frozen source and browser evidence

- Completion identity:
  `7c353166bb24b8a05e2219ab37f8d4b70dc2487b`
- Standalone artifact hash:
  `d34c67de8c4f2ac92e5f48797d7b82fd65c9d1303f5a1b7120461fb779ef56ea`
- Build source state: clean
- Regenerable evidence manifest:
  `artifacts/tearbench/checkpoints/pale-traverse/PT3-C6/white-hart-foundation/evidence.json`
- Captures: Phase I intro released, Phase II foundation, and Phase III
  high-contrast/reduced-motion/low-graphics/audio-off foundation.

The dedicated browser journey enters the real Boss Test mode, confirms Pale
stage index 4, observes the actual `white-hart` factory actor through all three
attack-unavailable phase ordinals, enters the ordinary defeat result screen,
and retries into the same boss/stage composition. The shared all-boss browser
journey also passes with White Hart included in the canonical live scenario
catalog.

## Verification

```text
pnpm typecheck
pnpm exec eslint <PT3-C6 changed TypeScript files>
pnpm exec vitest run <focused White Hart/factory/roster/lifecycle/reference/TearBench files>
pnpm check:architecture
pnpm check:active-roster
pnpm check:docs
pnpm check:terminology
pnpm requirements:check
pnpm check:game-reference
pnpm build:test:standalone
pnpm test:browser:pale-white-hart-foundation
pnpm test:browser:bosses
git diff --check
```

Result: the focused foundation set passed 52 tests, the source-derived
reference/TearBench set passed after its seven-boss expectations were updated,
typecheck and targeted lint passed, all repository authority gates above
passed, the clean standalone build passed, the dedicated White Hart journey
passed, and the shared boss journey exited 0.

## Evidence boundary

This checkpoint proves composition and lifecycle, not completed combat. Antler
Run, Snowbound Leap, Aurora Volley, Backtrail Kick, Ghost Tracks, Waystone
Turn, Frozen Wake, Hushed Crossing, Fracture Step, Crossing Storm, Endless
Return, and Last Crossing remain PT3-C7 work. The screenshots therefore show
the production body, arena, phases, accessibility profile, and Boss Test flow,
not finished attack choreography.

Joint Verdant/Pale integration, publication, reference/wiki dispatch,
deployment, final music selection, and C40 certification remain prohibited.
